# iris-classifier
The project builds a multiclass classifier based on feedforward neural networks (FFNN) to classify Iris dataset.

The FFNN uses ReLU activation in the hidden layers and Softmax in the ouput layer.
